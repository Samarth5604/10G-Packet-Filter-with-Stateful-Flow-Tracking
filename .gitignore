_build/
# packet_filter.opam is GENERATED by dune but must be COMMITTED --
# opam install . --deps-only reads it before dune has run.
.venv/
*.install
docs/protocol.md.new
# Vivado
*.jou
*.log
*.str
.Xil/
vivado*/
# reports/ IS committed: ADR 0006 makes a post-route timing report one of the
# five completion conditions, so it is evidence, not build output.
# reports/*.rpt